# Update from v0.6.2 to v0.6.3

- Same `applicationId` and same SharedPreferences file.
- Existing METAL DNA, reactions, Local Metal DB, Genre Lens, Last.fm and Spotify authentication data remain intact.
- Spotify verified-link cache switches to `spotify_artist_links_v063` so old identity decisions are rechecked once.
- Build with the same stable signing key and install over the existing APK.
