# Update from v0.6.3 to v0.6.4

- applicationId remains `jp.metaranai.app`.
- SharedPreferences remains `metaranai`.
- Existing METAL DNA, reactions, Local Metal DB, Genre Lens, Spotify tokens, and Last.fm API key are preserved.
- Spotify verified-link cache moves to `spotify_artist_links_v064`; older caches remain stored but are not trusted automatically.
- With the same signing key, install the release APK as a normal update.
